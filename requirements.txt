# uvicorn[standard]가 websockets를 포함하므로 WebSocket용 별도 의존성은 불필요.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.51
jinja2==3.1.5
qrcode[pil]==8.0
# [Week2] LLM 메시지 모더레이션(moderation.py)에서 Upstage Solar API 호출용.
# Solar는 OpenAI SDK와 호환되는 API를 제공하므로 openai 패키지를 그대로 사용한다.
openai==2.49.0
# [Week2] .env 파일에서 UPSTAGE_API_KEY 등 비밀값을 읽어 환경변수로 로드.
python-dotenv==1.2.2
# [Week3] POST /admin/qr-batch가 <form> 데이터(Form(...))를 받는 데 필요.
# FastAPI/Starlette는 폼 파싱을 이 패키지에 위임하며, 없으면 요청 시 즉시 RuntimeError.
python-multipart==0.0.20
