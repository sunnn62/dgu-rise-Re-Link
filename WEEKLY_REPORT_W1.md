# Re:Link 1주차 주간일지

**기간**: 2026-07-17 ~ 2026-07-22 (1주차) · **브랜치**: `develop`

---

## 이번 주 목표

PLAN.md 기준 1주차 목표는 크게 세 가지였습니다.

1. GAP A(부모가 활성화해야만 QR이 채팅으로 이어짐), GAP B(위치 공유 없이는 채팅 불가) 핵심 로직 구현
2. 보호자 인증을 "관리 링크 소유" 방식에서 "전화번호+PIN 로그인" 방식으로 전환
3. QR을 등록 시점이 아니라 사전에 배치로 인쇄해두고 시리얼 번호로 매칭하는 구조(QrToken) 도입

---

## 프론트엔드

| 항목 | 내용 |
|---|---|
| **보호자 자가 등록 페이지** | `/register` 페이지 신규 구현 |
| **보호자 관리 대시보드 UI** | 아이 목록을 보여주는 대시보드 화면 신규 구현 |
| **아이 카드 UI** | ON/OFF 토글(실종 신고 상태), 삭제 아이콘, QR 다운로드 버튼을 포함한 카드 컴포넌트 개편 |
| **등록/대시보드 라우트** | 백엔드 연동 전 인메모리 데이터로 화면 동작을 먼저 구현(목업) |
| **화면 스타일링** | 대시보드·등록 폼 레이아웃/스타일 작업 |
| **컬러 팔레트 확정** | 파스텔 색상 팔레트 + 오프화이트 배경으로 서비스 전체 톤 확정 |
| **발견 랜딩 페이지** | 버튼에 확정된 색상 클래스 반영 |

실제 DB 연동 전에 목업 데이터로 화면 골격(등록 폼, 대시보드, 아이 카드, 토글/삭제/QR 다운로드)을 먼저 완성해서, 백엔드 모델이 나오는 대로 바로 붙일 수 있는 상태를 만들어뒀습니다. 서비스 전체 톤(파스텔 팔레트, 오프화이트 배경)도 이번 주에 확정됐습니다.

## 백엔드

| 항목 | 내용 |
|---|---|
| **Guardian 로그인 체계** | 전화번호(ID) + PIN(6자리 이상) 로그인으로 전환. 세션 쿠키 기반 인증(`auth.py`), PIN은 PBKDF2-HMAC-SHA256(60만 회 반복) 해싱, 로그인 5회 연속 실패 시 15분 계정 잠금 |
| **QrToken 사전 발급 구조** | 등록 시점에 토큰을 즉석 발급하던 방식을 폐기하고, QR+시리얼 번호를 미리 배치로 만들어두는 구조로 변경. 배치 생성 스크립트(`generate_qr_batch.py`) 작성 — 헷갈리는 문자를 제외한 시리얼 생성, QR PNG·CSV 일괄 출력 |
| **GAP A (부모 활성화)** | `Child.status`(normal/missing) 필드 추가. 로그인한 보호자의 대시보드에서 토글, `/found/{qr_token}/start`가 `status != "missing"`이면 403으로 서버가 재검증 |
| **GAP B (위치 공유 필수)** | `ChatRoom.location_shared` 필드 추가. WebSocket 수신 루프에서 발견자가 위치를 먼저 보내지 않으면 텍스트 메시지를 거부, 위치 공유 시 즉시 잠금 해제 |
| **채팅방 소유권 검증** | 로그인 방식 전환으로 사라진 "방마다 발급되는 1회성 토큰"을 대체 — `/chat/{room_id}` HTTP 접근과 WebSocket 핸드셰이크 양쪽에서 `child.guardian_id == 로그인 세션의 guardian_id`를 매번 재검증. 다른 보호자가 room_id를 알아도 못 들어가도록 차단 |
| **API 계약 문서화** | [API_CONTRACT.md](API_CONTRACT.md) 작성 — 전체 라우트의 요청/응답/에러 코드, WebSocket 메시지 프로토콜을 정리해 프론트 팀원에게 전달 |
| **브랜치 통합** | 프론트 목업(develop)과 백엔드 구현을 병합해 `develop`에 통합 푸시. 프론트 커밋 히스토리는 그대로 보존, force push 없이 병합 |

## 통합 검증

코드 리뷰뿐 아니라 실제로 서버를 띄워 전체 흐름을 검증했습니다.

- 회원가입(시리얼 claim 포함) → 로그인 → 세션 쿠키 발급 → 대시보드 조회
- GAP A: 상태 토글 전에는 발견 신고 403, 토글 후 정상 진행 확인
- GAP B: 위치 공유 전 텍스트 차단 → 위치 공유 후 잠금 해제, WebSocket 레벨에서 직접 확인
- 채팅방 접근 권한: 발견자(공개) / 소유 보호자(성공) / 다른 보호자(HTTP·WS 양쪽에서 403) 세 가지 케이스 모두 의도대로 동작
- `develop` 병합 후 재검증까지 마치고 GitHub에 푸시 완료

---

## 남은 이슈 (다음 주로 이월)

- **112 신고 대체 경로 미구현** — 위치 공유를 거부한 발견자에게 `tel:112` 안내를 제공하는 화면/로직이 아직 없음
- **대시보드 시리얼 오류 미표시** — 백엔드는 에러 코드를 리다이렉트에 담아 보내지만, 대시보드 템플릿이 이를 읽어서 표시하지 않음
- PIN 재설정(OTP) 흐름 미구현
- 배포 도메인/HTTPS 미확정

## 다음 주 계획 (2주차)

- 목업 라우트를 완전히 대체하고 남은 프론트 오류 처리 UI 마무리 (시리얼/로그인/PIN 재설정)
- PIN 재설정(OTP) 흐름 + OTP 발송 rate limit 구현
- 배포 호스트 선정, 도메인 확정, HTTPS 세팅
- 도메인 확정 후 QrToken 정식 배치 생성 → QR 스티커 디자인 확정 및 인쇄 발주
- 실제 SMS API 연동 여부 결정
- 1차 통합 리허설
