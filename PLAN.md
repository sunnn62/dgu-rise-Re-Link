# Re:Link 실행 계획

**발견에서 연결까지 — 남은 3주 로드맵**

팀 4인(컴공 2 · 비전공 2) · 기간 3주 · 현재 상태: 저장소 루트에 프로토타입 동작 중

| 치명적 갭 | 뼈대 구현률 | 배포된 도메인 | 남은 기간 |
|---|---|---|---|
| 2건 | 약 70% | 0 | 3주 |

---

## 지금까지 확정된 결정

논의 과정에서 정리된 결정 사항입니다. 아래 항목은 더 이상 "열린 질문"이 아니라 실행 전제로 취급합니다.

- **아이 등록 주체**: 관리자 API가 아니라 **보호자가 직접 자가 등록**한다.
- **다자녀 지원**: 필요하다. 보호자 한 명이 아이를 여러 명 등록할 수 있으므로, "아이 1명 = 관리 링크 1개" 구조를 그대로 쓰면 안 되고 **보호자(Guardian) 단위로 묶은 구조**가 필요하다. (아래 [다자녀 지원](#다자녀-지원-guardian-엔티티) 참고)
- **보호자 인증 강도**: 일단 **관리 링크 소유 = 인증**으로 단순화한다. OTP 등 강한 인증은 3주 범위 밖으로 보류하고, README/발표에 "실서비스 전환 시 보완 필요"라고 명시한다.
- **위치 공유 거부 시 처리**: 채팅을 강제하지 않고 **112 신고 대체 경로**를 제공한다. (아래 GAP B 참고)

---

## 갭 분석

### GAP A — 부모가 활성화하기 전엔 QR이 죽어있어야 함

| | 내용 |
|---|---|
| 지금 | `/found/{qr_token}`에서 아무나 "발견했어요"를 누르면 즉시 채팅방이 생성됩니다. 평상시/실종 상태 구분이 없습니다. |
| 필요 | 아이 상태에 `normal / missing` 값이 있어야 하고, `missing`일 때만 QR 스캔이 채팅으로 이어져야 합니다. |
| 해결 방향 | `Child.status` 필드 추가 + 보호자(Guardian) 단위 관리 링크에서 아이별로 "실종 신고 켜기/끄기" 토글. |
| 중요 | 프론트에서만 막으면 우회됩니다. `/found/{qr_token}/start`와 `/ws/chat` 양쪽에서 서버가 `status == "missing"`을 재검증해야 합니다. |

### GAP B — 위치 전송이 곧 채팅 입장권이어야 함 (단, 대체 경로 있음)

| | 내용 |
|---|---|
| 지금 | 위치 공유는 발견자 화면의 선택 버튼입니다. 위치 없이도 텍스트 채팅이 가능합니다. |
| 필요 | 발견자는 위치를 먼저 보내야 텍스트 채팅이 열려야 합니다. (보호자는 예외) |
| 해결 방향 | `ChatRoom.location_shared`(bool) 추가. `role == "finder"`이고 `location_shared == False`인 상태로 `type: "text"`가 오면 서버가 거부. |
| 위치 거부 시 | 채팅 화면 진입 시 두 버튼 제시: **"위치 공유하고 채팅 시작"** / **"위치 공유 없이 112 신고하기"**(`tel:112`). 후자를 고르면 채팅 입력창은 열지 않고 112 안내로 전환하되, 서버는 보호자에게 시스템 메시지로 "발견자가 위치 공유 없이 112 신고 경로로 안내받았습니다"를 보내 스캔 사실만은 알 수 있게 함. |

### 다자녀 지원 (Guardian 엔티티)

지금 `Child` 모델은 `guardian_phone`, `guardian_name`을 아이마다 각각 들고 있고, 관리 토큰도 아이(정확히는 채팅방)마다 따로 발급되는 구조입니다. 보호자가 아이를 여러 명 등록하면 이 구조로는 **아이마다 별도의 관리 링크**가 생겨 보호자가 링크를 여러 개 관리해야 하는 문제가 생깁니다.

**제안하는 변경**:
- `Guardian` 모델 신설: `id`, `phone`, `name`, `manage_token`
- `Child.guardian_id`로 `Guardian`을 참조 (기존 `guardian_phone`/`guardian_name` 필드는 제거)
- 보호자 자가 등록 시 입력한 전화번호로 기존 `Guardian`을 조회 → 있으면 재사용(같은 관리 링크 유지), 없으면 신규 생성 후 SMS로 관리 링크 최초 1회 발송
- 관리 페이지(`/guardian/{manage_token}`)는 **아이 목록**을 보여주고, 아이별로 상태 토글 + QR 다운로드 + "아이 추가 등록" 버튼 제공

> **주의**: 등록 시 입력한 전화번호가 실제로 그 사람 것인지 검증하지 않습니다(문자 인증 없이 그냥 텍스트 입력). 데모 범위에서는 허용하되, 실서비스 전환 시 반드시 보완이 필요합니다.

---

## 기술 백로그

| 우선순위 | 항목 | 설명 |
|---|---|---|
| P0 | Guardian 엔티티 + 다자녀 구조 | `Guardian` 모델 신설, `Child.guardian_id` FK, 전화번호 기준 기존/신규 보호자 판별 |
| P0 | 보호자 자가 등록 폼 | 공개 "아이 등록하기" 웹 폼 (이름, 보호자 전화번호 등 입력 → Guardian 조회/생성 → Child 생성) |
| P0 | 보호자 관리 대시보드 | `/guardian/{manage_token}`: 아이 목록, 아이별 실종 상태 토글, QR 다운로드, 아이 추가 |
| P0 | 위치 전송 필수화 | `ChatRoom.location_shared`, 서버 사이드로 finder 텍스트 메시지 차단 |
| P0 | 112 신고 대체 경로 | 위치 공유를 거부한 발견자에게 `tel:112` 안내 화면, 보호자에게는 시스템 메시지로 통지 |
| P0 | 서버사이드 상태 재검증 | `/found/{token}/start`, `/ws/chat` 양쪽에서 missing 상태·위치 여부 재확인 (프론트 우회 방지) |
| P1 | HTTPS 배포 | 브라우저 Geolocation API는 HTTPS(또는 localhost) 아니면 동작 안 함 |
| P1 | 반응형/실기기 점검 | 모바일 카메라로 QR 스캔 → 실제 흐름 테스트 |
| P2 | 실제 SMS API 연동 | `sms.py` 시그니처는 이미 맞춰둠. 쿨SMS/알리고 중 선택, 비용 확인 |
| P2 | 개인정보 암호화 + 관리자 인증 | 보호자 전화번호 평문 저장 개선, 남아있는 `/admin/*`은 개발용 시드 도구로만 사용 |
| P2 | 남용 방지 | 같은 QR로 채팅방 무한 생성 방지 (rate limit 또는 "이미 진행 중인 방 있으면 재사용") |

---

## 비기술 백로그

- **QR 스티커 실물 디자인** — 옷에 부착/프린팅 가능한 형태(방수 스티커 또는 열전사). 최종 배포 도메인이 정해진 후 인쇄해야 QR을 다시 찍는 일이 없음
- **서비스 로고·브랜드 톤** — Re:Link 워드마크, 랜딩/채팅/보호자 대시보드에 쓸 최소한의 비주얼 아이덴티티
- **화면 카피** — 안내 문구, 위치 요청 문구, 112 안내 문구, SMS 문자 내용
- **개인정보·아동 관련 법적 검토** — 실종아동법, 개인정보보호법 상 아동 정보·위치정보 수집 관련 최소 확인 (발표 Q&A 대비)
- **발표 시나리오·리허설** — 실물 QR 스티커를 실제로 스캔하는 라이브 데모 + 네트워크 문제 대비 백업 영상(촬영·편집은 본인 담당)

---

## 4인 역할 분배

컴공 2명이 핵심 로직을 맡되, 비전공 2명도 각자 영역에 가벼운 코딩 과제를 하나씩 끼워 넣어 실습 기회를 만듭니다. 컴공 팀원이 짝을 지어 리뷰/페어링합니다.

### 백엔드 · 컴공 (본인)
- GAP A/B 서버 로직 구현 (112 경로 포함)
- Guardian 엔티티, DB 스키마 변경, 상태 재검증
- 배포 환경 세팅(HTTPS 포함), 보안 점검(P2 항목)
- 데모/백업 영상 촬영·편집

### 프론트 · 컴공
- 보호자 자가 등록 폼, 관리 대시보드 UI
- 채팅 UI 위치-잠금 & 112 안내 화면
- QR 코드 연동, 반응형 점검, 실기기 테스트

### 기획·운영 · 비전공
- 발표 스크립트, 데모 시나리오
- 개인정보/법 관련 리스크 정리
- 리허설 진행 및 피드백 수집
- **코딩 실습**: 화면 카피·SMS·112 안내 문구를 템플릿(`.html`)에 직접 넣고 수정 — 프론트 담당과 페어링

### 디자인·브랜드 · 비전공
- QR 스티커 실물 디자인·제작
- 로고/브랜드 비주얼, 발표자료(PPT) 디자인
- 부착 시연 소품 준비
- **코딩 실습**: `style.css`에서 색상·버튼·간격 값 직접 조정해 디자인 시안 반영 — 프론트 담당과 페어링

---

## 3주 일정

### Week 1 — 핵심 갭 & 데이터 모델
- Guardian 엔티티 설계, 스키마 변경 합의
- GAP A/B 백엔드 구현 (활성화, 위치 필수화, 112 경로)
- 로고·톤 방향 스케치 시작, 발표 시나리오 초안

### Week 2 — 통합 & 실물 제작
- 보호자 자가 등록 폼 + 관리 대시보드 완성
- 도메인 확정 → 배포 환경 세팅
- QR 스티커 디자인 확정, 인쇄 발주
- SMS 실제 연동 여부 결정
- 1차 통합 리허설

### Week 3 — 마감 & 리허설
- 배포 완료, 실기기 최종 점검
- 보안 점검(개인정보, admin 접근 제한)
- 발표자료 완성
- 실물 스티커로 라이브 데모 리허설 + 백업 데모 영상 촬영

---

## 개인별 3주 실행 계획

역할별 담당 항목을 사람 단위로 쪼갠 실행 리스트입니다. 서로 넘겨받는 지점(🔗 의존)을 놓치면 뒷사람 일정이 밀리니 특히 주의하세요.

### 본인 · 백엔드

**Week 1**
- [ ] `Guardian` 모델 신설 (`id`, `phone`, `name`, `manage_token`), `Child.guardian_id` FK로 연결, 기존 `guardian_phone`/`guardian_name` 필드 정리
- [ ] `Child.status`(`normal`/`missing`) 필드 추가
- [ ] `ChatRoom.location_shared`(bool) 필드 추가
- [ ] WebSocket 수신 루프: finder의 `location_shared == False` 상태에서 text 메시지 오면 차단
- [ ] `/found/{token}/start`, `/ws/chat`에서 `status == "missing"` 서버사이드 재검증
- [ ] 프론트 팀원과 API 계약(엔드포인트/응답 스키마) 초안 공유 — 늦어도 주 후반까지
- 🔗 **넘겨줄 것**: 보호자 등록/토글/조회용 API 스펙(엔드포인트, 요청·응답 필드) → 프론트 팀원이 Week 2에 바로 붙일 수 있게

**Week 2**
- [ ] 보호자 자가 등록 백엔드 로직 (전화번호로 기존 `Guardian` 조회 → 재사용/신규 생성 → `Child` 생성)
- [ ] 112 신고 대체 경로 서버 처리 (finder가 위치 거부 시 시스템 메시지로 보호자에게 통지)
- [ ] 배포 호스트 선정 및 환경 세팅(HTTPS 포함), `BASE_URL` 배포 도메인으로 교체
- [ ] 프론트와 1차 통합 — 실제 API로 대시보드/등록 폼 연결 확인
- 🔗 **넘겨줄 것**: 확정된 배포 도메인 → 디자인 팀원이 QR 스티커 인쇄 진행할 수 있게 (늦어도 주 중반)

**Week 3**
- [ ] 배포 최종 점검(WebSocket 프록시, DB 영속성)
- [ ] 보안 점검: 개인정보 저장, `/admin/*` 접근 제한, 남용 방지(rate limit)
- [ ] 실기기 통합 테스트, 발견된 버그 수정
- [ ] 데모/백업 영상 촬영 및 편집
- [ ] 최종 리허설 기술 지원

---

### 프론트 · 컴공

**Week 1**
- [ ] 백엔드 팀원과 API 계약 정렬(엔드포인트/응답 스키마) — 이후 화면 개발의 기준
- [ ] 보호자 자가 등록 폼 마크업(임시 데이터로 우선 개발)
- [ ] 보호자 대시보드 스켈레톤(아이 목록 + 상태 토글 UI, 목업 데이터)
- [ ] `chat.html` 위치-잠금 UI 및 "위치 공유 / 112 신고" 두 버튼 화면 작업
- 🔗 **받을 것**: 백엔드의 API 스펙 (Week 1 후반)

**Week 2**
- [ ] 목업 데이터를 실제 백엔드 API로 교체(등록 폼, 대시보드, 상태 토글 연동)
- [ ] QR 코드 다운로드/표시를 대시보드에 통합
- [ ] 기획·운영 팀원과 페어링 — 화면 카피/112 안내 문구를 템플릿에 함께 삽입 (코딩 실습)
- [ ] 디자인 팀원과 페어링 — 확정된 로고/컬러를 `style.css`에 함께 반영 (코딩 실습)
- [ ] 반응형 1차 점검
- 🔗 **넘겨줄 것**: 동작하는 등록/대시보드 화면 → 기획팀 리허설에서 실제로 써볼 수 있게

**Week 3**
- [ ] 여러 실기기(안드로이드/아이폰, 카메라 앱 다른 버전)로 QR 스캔 → 전체 흐름 테스트
- [ ] 통합 테스트에서 나온 버그 수정
- [ ] 최종 배포본 UI 점검, 리허설 기술 지원

---

### 기획·운영 · 비전공

**Week 1**
- [ ] 발표 시나리오 초안 (문제 정의 → 데모 흐름 → 마무리)
- [ ] 실종아동법·개인정보보호법 관련 이슈 한 페이지로 정리 (발표 Q&A 대비)
- [ ] 핵심 화면 카피 초안 작성: 랜딩 안내문구, 위치 요청 문구, 112 안내 문구, SMS 문자 내용 (문서로 먼저 정리)
- 🔗 **넘겨줄 것**: 카피 초안 → 프론트 팀원과의 Week 2 페어링에 사용

**Week 2**
- [ ] 프론트 팀원과 페어링 — 정리한 카피를 템플릿(`.html`)에 직접 삽입 (코딩 실습)
- [ ] 법적/개인정보 요약을 Q&A 예상질문-답변 형태로 다듬기
- [ ] 1차 통합 리허설 진행 — 일정 조율, 시나리오대로 실제 흐름 실행, 어색한 지점 기록
- [ ] 리허설 피드백을 팀에 공유하고 우선순위 조정 제안

**Week 3**
- [ ] 발표 스크립트 최종본 완성
- [ ] 리허설 반복 진행, 시간 재기, Q&A 답변 다듬기
- [ ] 백업 데모 영상 내레이션/자막 문구 지원 (편집은 본인/백엔드 담당)
- [ ] 발표 당일 역할(발표자/시연자/QR 부착 담당) 확정

---

### 디자인·브랜드 · 비전공

**Week 1**
- [ ] 로고/브랜드 톤 2~3개 시안 스케치
- [ ] QR 스티커 컨셉 스케치 (실물 소재: 방수 스티커 vs 열전사 프린팅 후보 조사)
- [ ] 발표자료(PPT) 기본 골격/템플릿 잡기
- ⚠️ QR 스티커 최종 인쇄는 **배포 도메인이 확정된 후**에만 가능 — Week 1에는 컨셉까지만

**Week 2**
- [ ] 로고/컬러 확정 (프론트 팀원에게 전달)
- [ ] 🔗 **받는 즉시**: 백엔드가 확정한 배포 도메인으로 QR 스티커 최종 디자인 완성 → **인쇄 발주** (인쇄 리드타임 고려해 주 중반까지는 발주)
- [ ] 프론트 팀원과 페어링 — `style.css`에서 색상/버튼/간격 값 직접 조정 (코딩 실습)
- [ ] 대시보드/채팅 화면 실제 스크린샷으로 발표자료 슬라이드 채우기 시작

**Week 3**
- [ ] 인쇄된 스티커 실물 수령 → 옷/천에 실제 부착 테스트
- [ ] 발표자료 최종 스크린샷/플로우로 완성
- [ ] 시연 소품(스티커 부착된 옷) 준비
- [ ] 리허설 참여 — 소품 담당 및 발표자료 큐 지원

---

## 발표 당일 "실사용 가능"의 최소 조건

- [ ] 고정 도메인 확정 (Render/Railway/Fly.io 무료 티어 또는 구매 도메인) — **QR 인쇄 전에 반드시 먼저 확정**
- [ ] HTTPS 적용 — Geolocation API는 HTTPS 또는 localhost가 아니면 브라우저가 막음
- [ ] `main.py`의 `BASE_URL`을 실제 배포 도메인으로 교체
- [ ] WebSocket이 배포 환경에서도 열리는지 확인 (일부 무료 호스팅은 WS 프록시 설정 별도 필요)
- [ ] DB 파일이 재배포/재시작 시 날아가지 않는지 확인 (영구 볼륨 또는 배포 직전 최종본 고정)
- [ ] 실물 QR 스티커로 실제 스마트폰 카메라 스캔 테스트 (에뮬레이터 아닌 실기기)

---

## 남은 열린 질문

- **실제 SMS API 도입 여부** — 데모는 콘솔 로그 목업으로 충분할 수도 있음. 실사용을 강조하려면 비용을 감수하고 붙일지 팀 논의 필요
