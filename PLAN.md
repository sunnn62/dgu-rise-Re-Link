# Re:Link 실행 계획

**발견에서 연결까지 — 남은 3주 로드맵**

팀 4인(컴공 2 · 비전공 2) · 기간 3주 · 현재 상태: 저장소 루트에 프로토타입 동작 중

| 치명적 갭 | 뼈대 구현률 | 배포된 도메인 | 남은 기간 |
|---|---|---|---|
| 2건 | 약 70% | 0 | 3주 |

---

## 지금까지 확정된 결정

논의 과정에서 정리된 결정 사항입니다. 아래 항목은 더 이상 "열린 질문"이 아니라 실행 전제로 취급합니다.

- **아이 등록 주체**: 관리자 API가 아니라 **보호자가 직접 자가 등록**한다.
- **다자녀 지원**: 필요하다. 보호자 한 명이 아이를 여러 명 등록할 수 있으므로, "아이 1명 = 관리 링크 1개" 구조를 그대로 쓰면 안 되고 **보호자(Guardian) 단위로 묶은 구조**가 필요하다. (아래 [다자녀 지원](#다자녀-지원-guardian-엔티티) 참고)
- **보호자 인증 강도**: ~~관리 링크 소유 = 인증으로 단순화~~ → **전화번호(ID) + PIN(비밀번호) 로그인 방식**으로 변경. (아래 [보호자 인증 방식](#보호자-인증-방식-로그인pin) 참고) 관리 링크(`manage_token`) 자체는 **모델에서 제거**한다 — 로그인 세션이 유일한 접근 수단이 되고, QR 다운로드 등은 `Child`/`QrToken`의 `qr_token`으로 이미 충분히 처리되므로 보호자 레벨의 별도 토큰은 불필요.
- **위치 공유 거부 시 처리**: 채팅을 강제하지 않고 **112 신고 대체 경로**를 제공한다. (아래 GAP B 참고)
- **QR 발급 방식**: 등록하는 순간 토큰을 즉석 발급하지 않고, **QR(+시리얼 번호)을 미리 배치로 인쇄해두고 로그인한 보호자가 아이를 추가할 때 시리얼 번호를 입력해 매칭(claim)**하는 방식으로 간다. (아래 [QR 사전 인쇄 & 시리얼 매칭](#qr-사전-인쇄--시리얼-매칭-qrtoken-풀) 참고)

---

## 이미 완료된 작업 (develop 브랜치)

`main`에는 아직 안 올라갔지만, `develop` 브랜치에 seonwoo님이 이미 만들어두신 것들입니다.

- ✅ `/register` — 보호자 자가 등록 페이지(이름, 전화번호 입력)
- ✅ `/guardian/{manage_token}` — 보호자 대시보드: 아이 카드 UI(이름, **ON/OFF 토글로 실종 신고 상태 전환** — GAP A의 화면 부분에 해당, 삭제 아이콘, QR 다운로드 버튼), "아이 추가" 폼
- ✅ 관련 라우트 일체 (`main.py`에 `/register`, `/guardian/{manage_token}`, 아이 추가/토글/삭제) — 단, **인메모리 딕셔너리 기반 목업**이라 서버 재시작하면 데이터가 날아가고, 아직 실제 `Guardian`/`Child`/`QrToken` DB 모델과 연결돼 있지 않음
- ✅ 파스텔 색상 팔레트 + 오프화이트 배경 등 전체 스타일 톤 확정, 발견 랜딩 페이지 버튼에도 반영

**로그인 방식으로 바뀌면서 추가로 손봐야 할 부분**: 이 목업은 애초에 "관리 링크 소유 = 인증" 기준으로 만들어져서, 로그인+PIN 방식과는 화면 구조가 일부 안 맞습니다.

- `register.html`에 **PIN 입력란**을 추가해야 함 (지금은 이름/전화번호만 있음)
- **로그인 페이지(`login.html`)가 아직 없음** — 새로 만들어야 함
- 대시보드 라우팅을 `/guardian/{manage_token}`(URL에 토큰 노출) 방식에서 `/guardian/dashboard`(세션 쿠키 기반) 방식으로 바꿔야 함
- 아이 카드 UI(ON/OFF 토글, 삭제, QR 다운로드)와 "아이 추가" 폼 마크업 자체는 **그대로 재사용 가능** — 로그인 세션만 앞단에 씌우면 됨

---

## 갭 분석

### GAP A — 부모가 활성화하기 전엔 QR이 죽어있어야 함

| | 내용 |
|---|---|
| 지금 | `/found/{qr_token}`에서 아무나 "발견했어요"를 누르면 즉시 채팅방이 생성됩니다. 평상시/실종 상태 구분이 없습니다. |
| 필요 | 아이 상태에 `normal / missing` 값이 있어야 하고, `missing`일 때만 QR 스캔이 채팅으로 이어져야 합니다. |
| 해결 방향 | `Child.status` 필드 추가 + **로그인한 보호자의 대시보드**에서 아이별로 "실종 신고 켜기/끄기" 토글. |
| 중요 | 프론트에서만 막으면 우회됩니다. `/found/{qr_token}/start`와 `/ws/chat` 양쪽에서 서버가 `status == "missing"`을 재검증해야 합니다. |

### GAP B — 위치 전송이 곧 채팅 입장권이어야 함 (단, 대체 경로 있음)

| | 내용 |
|---|---|
| 지금 | 위치 공유는 발견자 화면의 선택 버튼입니다. 위치 없이도 텍스트 채팅이 가능합니다. |
| 필요 | 발견자는 위치를 먼저 보내야 텍스트 채팅이 열려야 합니다. (보호자는 예외) |
| 해결 방향 | `ChatRoom.location_shared`(bool) 추가. `role == "finder"`이고 `location_shared == False`인 상태로 `type: "text"`가 오면 서버가 거부. |
| 위치 거부 시 | 채팅 화면 진입 시 두 버튼 제시: **"위치 공유하고 채팅 시작"** / **"위치 공유 없이 112 신고하기"**(`tel:112`). 후자를 고르면 채팅 입력창은 열지 않고 112 안내로 전환하되, 서버는 보호자에게 시스템 메시지로 "발견자가 위치 공유 없이 112 신고 경로로 안내받았습니다"를 보내 스캔 사실만은 알 수 있게 함. |
| ⚠️ role 판정 + 방 소유권 | `role`을 클라이언트가 자체 선언하는 값으로 신뢰하면 안 됩니다. 특히 로그인 방식으로 바뀌면서 예전의 "방마다 발급되는 `guardian_token`" 이 사라졌으므로, **로그인한 보호자 A가 `room_id`만 알면 보호자 B의 아이 채팅방에 들어갈 수 있는 문제**가 새로 생깁니다. `/chat/{room_id}?role=guardian` 진입 및 해당 WebSocket 핸드셰이크에서 반드시 `ChatRoom.child.guardian_id == 현재 세션의 guardian_id`를 재검증해야 합니다. |

### 보호자 인증 방식 (로그인+PIN)

기존 계획("관리 링크 소유 = 인증", "전화번호로 기존 Guardian 조회 후 재사용")은 두 가지 문제가 있었습니다.

1. **모순**: "관리 링크를 가진 사람만 접근 가능"이라고 정해놓고도, 실제로는 전화번호만 입력하면 기존 Guardian을 찾아 새 아이를 그 계정에 그대로 붙일 수 있었습니다. 즉 제3자가 남의 전화번호(추측/유출 포함)와 미사용 QR 시리얼만 있으면, 원래 보호자의 동의나 알림 없이 그 사람 계정에 아이를 추가할 수 있는 구조였습니다.
2. **UX**: 관리 링크는 아이 등록·실종 신고 때 외엔 거의 쓰지 않아 분실/재발급 문제가 실제로 자주 발생할 것으로 예상됩니다.

**변경된 방식**: 전화번호를 ID로, **PIN(6자리 이상 숫자)을 비밀번호로** 쓰는 로그인 방식으로 전환합니다.

- **회원가입(최초 등록)**: 이름 + 전화번호 + PIN + QR 스티커 시리얼 번호 입력 → 신규 `Guardian` 생성, PIN은 해싱해서 저장 (실제 구현: `bcrypt`/`argon2` 대신 표준 라이브러리 `hashlib.pbkdf2_hmac`(SHA-256, 60만 회 반복) 사용 — 무거운 외부 의존성을 피하면서 OWASP 권장 반복 횟수를 충족하는 대체. `auth.py` 참고)
- **로그인**: 전화번호 + PIN → 세션 쿠키 발급 → `/guardian/dashboard`에서 본인이 등록한 아이 **목록을 테이블 형태**로 확인
- **아이 추가 등록**: 전화번호 재조회로 기존 계정에 붙이지 않고, **로그인한 상태에서 대시보드 내 "아이 추가" 버튼**으로만 진행 (시리얼 번호 입력은 그대로 필요)
- **진행 중인 채팅방 접근**: SMS 직접 링크 방식은 더 이상 안 쓰므로, 알림 SMS는 "로그인해서 확인하세요" 안내로 바꾸고 **대시보드에 아이별 "진행 중인 채팅방" 표시 + 입장 버튼**을 추가한다. 아이가 `missing` 상태에서 발견 신고(`ChatRoom` 생성)가 들어오면 대시보드에 바로 노출.
- **PIN 재설정**: 로그인 화면 "PIN을 잊으셨나요?" → 전화번호 입력 → SMS OTP 발송/확인 → PIN 재설정. **아이디(전화번호) 찾기 기능은 스코프에서 제외** — 필요시 "고객센터 문의"로 README/Q&A에만 명시.
- **로그인 시도 제한**: **5회 연속 실패 시 해당 전화번호 계정을 15분간 잠금.** PIN이 짧은 만큼 이게 없으면 사실상 인증이 없는 것과 같습니다.
- **OTP 발송 자체도 남용 방지 필요**: "PIN 재설정" 요청은 로그인 없이 아무나 아무 번호로 누를 수 있으므로, 같은 번호로 **60초 내 재요청 불가 + 1시간 내 최대 5회**로 제한합니다(안 그러면 임의의 번호로 SMS 스팸/비용 유발이 가능). 발송된 OTP는 **5분 후 만료 + 1회 사용 후 즉시 무효화**합니다.

> **가입 시 중복 전화번호 응답에 대한 의도적 트레이드오프**: 가입 폼에서 이미 등록된 번호를 입력하면 "이미 가입된 번호입니다, 로그인해주세요"라고 바로 알려줍니다. 이는 특정 번호가 가입돼 있는지 아닌지를 제3자가 알아낼 수 있는 계정 열거(enumeration) 여지를 남기지만, UX 상 감수하기로 한 트레이드오프입니다. 실서비스 전환 시에는 응답 문구를 통일하는 방향을 검토합니다.

> **PIN 자체에 대한 주의**: PIN은 표준 비밀번호 정책(대소문자·특수문자 등)을 요구하지 않는 가벼운 방식입니다. 위협 모델(금융/의료 데이터가 아닌, 아이 이름·실종 상태·위치 공유 여부)에 비춰 3주 데모 범위에서는 충분하다고 판단했지만, **6자리 이상 + rate limit + 해싱** 세 가지가 세트로 갖춰져야 최소 조건을 만족합니다. 실서비스 전환 시 2FA나 표준 비밀번호 정책으로 강화가 필요하다고 README/발표에 명시합니다.

### 다자녀 지원 (Guardian 엔티티)

지금 `Child` 모델은 `guardian_phone`, `guardian_name`을 아이마다 각각 들고 있고, 관리 토큰도 아이(정확히는 채팅방)마다 따로 발급되는 구조입니다. 보호자가 아이를 여러 명 등록하면 이 구조로는 **아이마다 별도의 관리 접근 수단**이 생겨 보호자가 여러 개를 따로 관리해야 하는 문제가 생깁니다.

**제안하는 변경**:
- `Guardian` 모델 신설: `id`, `phone`(로그인 ID), `pin_hash`, `name` — **`manage_token`은 두지 않음** (로그인 세션이 유일한 접근 수단)
- `Child.guardian_id`로 `Guardian`을 참조 (기존 `guardian_phone`/`guardian_name` 필드는 제거)
- **최초 등록**: 이름 + 전화번호 + PIN + QR 시리얼 번호 입력 → 신규 `Guardian` 생성 (전화번호 중복 시 "이미 가입된 번호입니다, 로그인해주세요" 안내)
- **아이 추가 등록**: 반드시 로그인한 세션에서만 가능. 전화번호로 기존 계정을 자동 조회/매칭하지 않음
- 로그인 후 대시보드(`/guardian/dashboard`)는 **아이 목록을 테이블**로 보여주고, 아이별로 상태 토글 + QR 다운로드 + 진행 중인 채팅방 입장 + "아이 추가 등록" 버튼 제공
- **채팅방 접근 시 반드시 `child.guardian_id == 로그인 세션의 guardian_id`를 재검증** (GAP B 참고 — 다른 보호자의 아이 방에 들어가지 못하도록)

> **주의**: 회원가입 시 입력한 전화번호가 실제로 그 사람 것인지는 별도 검증하지 않습니다(가입 단계 SMS 인증 없이 텍스트 입력). 데모 범위에서는 허용하되, PIN 재설정 시의 OTP 확인이 최소한의 사후 검증 역할을 합니다. 실서비스 전환 시 가입 단계 인증 보완이 필요합니다.

### QR 사전 인쇄 & 시리얼 매칭 (QrToken 풀)

기존 계획은 "보호자가 등록하는 순간 서버가 새 토큰을 만들고 그걸로 QR을 생성"하는 방식이었습니다. 이러면 **QR을 등록 이후에만 만들 수 있어서** 스티커를 미리 대량 인쇄해둘 수 없고, 발표 부스에서 낯선 참가자에게 "미리 만들어둔 실물 스티커"를 즉석에서 건네줄 수도 없습니다.

**변경된 방식**: QR(토큰)과 사람이 읽고 입력할 수 있는 **시리얼 번호**를 등록과 무관하게 미리 배치로 만들어두고, 보호자는 (로그인 후) 아이를 추가할 때 스티커에 적힌 시리얼 번호를 입력해서 그 QR을 자기 아이에게 연결(활성화)합니다.

**모델**: `QrToken`
- `id`, `qr_token`(URL에 쓰이는 긴 랜덤 문자열, 기존 `Child.qr_token`과 같은 역할), `serial`(스티커에 인쇄할 짧은 코드 — `0/O`, `1/I/l`처럼 헷갈리는 문자는 제외), `child_id`(처음엔 `NULL`), `claimed_at`

**흐름**:
1. (배치 생성) 도메인이 확정되면, 스크립트로 `QrToken`을 N개 미리 생성 → 각 항목의 `qr_token`으로 QR PNG 일괄 생성(기존 `qr_utils.generate_qr_png` 재사용) + 시리얼 목록을 디자인팀에 전달. **N은 예상 참가자 수 + 여유분(예: 20~30%) 기준으로 결정** — 너무 적으면 행사 중 시리얼 소진, 너무 많으면 미인쇄/미등록 QR이 낭비됨.
2. (인쇄) 디자인팀이 QR + 시리얼 번호를 스티커 시안에 배치해 인쇄 발주 — 등록 여부와 무관하게 한 번에 진행 가능
3. (등록/매칭) 로그인한 보호자가 대시보드의 "아이 추가" 폼에서 아이 이름과 함께 시리얼 번호 입력 → 서버가 `QrToken`에서 조회
   - 없는 시리얼 → 404 "유효하지 않은 시리얼 번호입니다"
   - 이미 `child_id`가 있음 → 409 "이미 등록된 QR입니다"
   - 미사용 → `Child` 생성 후 `QrToken.child_id`/`claimed_at` 채우고, `Child.qr_token`에 `QrToken.qr_token` 값을 그대로 복사

> `Child.qr_token`을 그대로 유지하는 이유: `/found/{qr_token}` 랜딩, 채팅, WebSocket 등 기존 로직이 전부 `Child.qr_token` 기준으로 조회하도록 짜여 있어서, 이 필드만 채워주면 나머지 코드는 손댈 필요가 없습니다.

---

## 기술 백로그

| 우선순위 | 항목 | 설명 |
|---|---|---|
| P0 | Guardian 엔티티 + 로그인 구조 | `Guardian` 모델 신설(`phone`, `pin_hash`) — `manage_token` 없음. `Child.guardian_id` FK, 회원가입/로그인/세션 처리 |
| P0 | PIN 인증 안전장치 | ✅ PIN 해싱(구현: PBKDF2-HMAC-SHA256 60만 회, `auth.py`) + 로그인 5회 실패 시 15분 잠금 완료. OTP 발송 rate limit(60초/1시간 5회) + 5분 만료·1회용은 Week 2 예정(PIN 재설정 기능과 함께) |
| P0 | QrToken 사전 발급 풀 | `QrToken` 모델(`qr_token`, `serial`, `child_id` nullable, `claimed_at`) + 배치 생성 스크립트(N개 미리 생성, QR PNG 일괄 출력) |
| P0 | 보호자 회원가입/로그인 폼 | `/register`는 develop 브랜치에 있으나 PIN 필드 추가 필요. 로그인 폼(`login.html`)은 신규 제작 |
| P0 | 보호자 대시보드 → 세션 전환 | 아이 카드 UI는 develop에 완성됨(재사용). 라우팅을 `/guardian/{manage_token}` → `/guardian/dashboard`(세션 기반)로 전환, 진행 중인 채팅방 입장 UI 추가 |
| P0 | 위치 전송 필수화 | `ChatRoom.location_shared`, 서버 사이드로 finder 텍스트 메시지 차단 |
| P0 | 112 신고 대체 경로 | 위치 공유를 거부한 발견자에게 `tel:112` 안내 화면, 보호자에게는 시스템 메시지로 통지 |
| P0 | 서버사이드 상태/소유권 재검증 | `/found/{token}/start`, `/ws/chat` 양쪽에서 missing 상태·위치 여부 재확인 + **채팅방-보호자 소유권 검증**(`child.guardian_id == session.guardian_id`) |
| P1 | HTTPS 배포 | 브라우저 Geolocation API는 HTTPS(또는 localhost) 아니면 동작 안 함 |
| P1 | 반응형/실기기 점검 | 모바일 카메라로 QR 스캔 → 실제 흐름 테스트 |
| P2 | 실제 SMS API 연동 | 가입 알림용은 아니어도, PIN 재설정 OTP는 최소 콘솔 로그 목업으로라도 필요. 실제 연동은 쿨SMS/알리고 중 선택, 비용 확인 |
| P2 | 개인정보 암호화 + 관리자 인증 | 보호자 전화번호 평문 저장 개선, 남아있는 `/admin/*`은 개발용 시드 도구로만 사용 |
| P2 | 남용 방지 | 같은 QR로 채팅방 무한 생성 방지 (rate limit 또는 "이미 진행 중인 방 있으면 재사용") |

---

## 비기술 백로그

- **QR 스티커 실물 디자인** — 옷에 부착/프린팅 가능한 형태(방수 스티커 또는 열전사). QR과 함께 **시리얼 번호를 사람이 읽기 쉬운 글씨체로 병기**해야 함. 도메인 확정 후 배치로 한 번에 인쇄 가능(등록 완료를 기다릴 필요 없음)
- **서비스 로고·브랜드 톤** — develop 브랜치에 파스텔 컬러 팔레트 + 오프화이트 배경이 이미 style.css에 반영돼 있음. 처음부터 새로 정하기보다 **이 톤에 맞춰 로고/QR 스티커 디자인을 확장**하는 방향으로 진행
- **화면 카피** — 안내 문구, 위치 요청 문구, 112 안내 문구, SMS(가입 안내/OTP) 문자 내용
- **개인정보·아동 관련 법적 검토** — 실종아동법, 개인정보보호법 상 아동 정보·위치정보 수집 및 PIN/전화번호 저장 관련 최소 확인 (발표 Q&A 대비, "왜 비밀번호가 아니라 PIN이냐" 답변 포함)
- **발표 시나리오·리허설** — 실물 QR 스티커를 실제로 스캔하는 라이브 데모 + 네트워크 문제 대비 백업 영상(촬영·편집은 본인 담당)

---

## 4인 역할 분배

컴공 2명이 핵심 로직을 맡되, 비전공 2명도 각자 영역에 가벼운 코딩 과제를 하나씩 끼워 넣어 실습 기회를 만듭니다. 컴공 팀원이 짝을 지어 리뷰/페어링합니다.

### 백엔드 · 컴공 (본인)
- GAP A/B 서버 로직 구현 (112 경로, role/소유권 재검증 포함)
- Guardian 엔티티, 로그인/세션/PIN 해싱/rate limit, `QrToken` 사전 발급 풀 + 배치 생성 스크립트, DB 스키마 변경
- 배포 환경 세팅(HTTPS 포함), 보안 점검(P2 항목)
- 데모/백업 영상 촬영·편집

### 프론트 · 컴공
- 보호자 회원가입 폼에 PIN 필드 추가, 로그인 폼 신규 제작, 대시보드를 세션 기반으로 전환 + 진행 중인 채팅방 UI 추가 (아이 카드 UI 자체는 develop에서 재사용)
- 채팅 UI 위치-잠금 & 112 안내 화면
- QR 코드 연동, 반응형 점검, 실기기 테스트

### 기획·운영 · 비전공
- 발표 스크립트, 데모 시나리오
- 개인정보/법 관련 리스크 정리 (PIN 인증 방식 Q&A 답변 포함)
- 리허설 진행 및 피드백 수집
- **코딩 실습**: 화면 카피·SMS·112 안내 문구를 템플릿(`.html`)에 직접 넣고 수정 — 프론트 담당과 페어링

### 디자인·브랜드 · 비전공
- QR 스티커 실물 디자인·제작
- 로고/브랜드 비주얼, 발표자료(PPT) 디자인
- 부착 시연 소품 준비
- **코딩 실습**: `style.css`에서 색상·버튼·간격 값 직접 조정해 디자인 시안 반영 — 프론트 담당과 페어링

---

## 3주 일정

### Week 1 — 핵심 갭 & 데이터 모델 & 인증 구조
- Guardian 엔티티, 로그인/PIN/세션 구조, QrToken 사전 발급 풀 설계, 스키마 변경 합의
- GAP A/B 백엔드 구현 (활성화, 위치 필수화, 112 경로, role/소유권 판정 방식 확정)
- 로고·톤 방향 스케치 시작, 발표 시나리오 초안

### Week 2 — 통합 & 실물 제작
- 보호자 회원가입/로그인 폼(PIN·시리얼 번호 매칭 포함) + 세션 기반 대시보드 완성
- PIN 재설정(OTP) 흐름, 로그인/OTP rate limit 적용
- 도메인 확정 → 배포 환경 세팅 → QrToken 배치 생성(N개)
- QR 스티커(QR+시리얼) 디자인 확정, 인쇄 발주
- SMS 실제 연동 여부 결정 (최소 OTP 콘솔 로그 목업은 확보)
- 1차 통합 리허설

### Week 3 — 마감 & 리허설
- 배포 완료, 실기기 최종 점검
- 보안 점검(PIN 해싱, rate limit 동작 확인, 채팅방 소유권 검증, 개인정보, admin 접근 제한)
- 발표자료 완성
- 실물 스티커로 라이브 데모 리허설 + 백업 데모 영상 촬영

---

## 개인별 3주 실행 계획

역할별 담당 항목을 사람 단위로 쪼갠 실행 리스트입니다. 서로 넘겨받는 지점(🔗 의존)을 놓치면 뒷사람 일정이 밀리니 특히 주의하세요.

### 본인 · 백엔드

**Week 1**
- [x] `Guardian` 모델 신설 (`id`, `phone`, `pin_hash`, `name`), `Child.guardian_id` FK로 연결, 기존 `guardian_phone`/`guardian_name` 필드 정리 — 완료
- [x] 회원가입/로그인/세션 로직: PIN 해싱, 로그인 5회 실패 시 15분 잠금 — 완료 (해싱은 `bcrypt`/`argon2` 대신 `hashlib.pbkdf2_hmac` 60만 회 반복으로 구현, `auth.py`)
- [x] `QrToken` 모델 신설 (`qr_token`, `serial`, `child_id` nullable, `claimed_at`) + 배치 생성 스크립트(`generate_qr_batch.py`) — 완료
- [x] `Child.status`(`normal`/`missing`) 필드 추가 — 완료
- [x] `ChatRoom.location_shared`(bool) 필드 추가 — 완료
- [x] WebSocket 수신 루프: finder의 `location_shared == False` 상태에서 text 메시지 오면 차단 — 완료
- [x] `role` 판정 + **채팅방 소유권 검증**(`child.guardian_id == session.guardian_id`) 방식 확정 및 구현 — 완료 (HTTP `/chat/{room_id}`, WS 핸드셰이크 양쪽)
- [x] `/found/{token}/start`, `/ws/chat`에서 `status == "missing"` 서버사이드 재검증 — 완료
- [x] 아이 삭제 로직도 실제 구현 — 완료 (QrToken unclaim까지 처리)
- [x] 프론트 팀원과 API 계약 공유 — [API_CONTRACT.md](API_CONTRACT.md) 작성 완료
- 🔗 **넘겨줄 것**: 회원가입/로그인/대시보드용 API 스펙(엔드포인트, 요청·응답 필드) → 프론트 팀원이 Week 2에 바로 붙일 수 있게 — `API_CONTRACT.md`로 전달 완료

**Week 2**
- [ ] 보호자 회원가입 백엔드 로직 (전화번호 중복 체크 → PIN 해싱 저장 → 입력받은 시리얼로 `QrToken` 조회·검증(미존재/이미 사용됨 처리) → `Child` 생성 + `QrToken` claim)
- [ ] 로그인 후 세션 기반 "아이 추가 등록" 로직 (전화번호 자동 매칭 방식 완전히 제거)
- [ ] PIN 재설정 흐름: 전화번호 입력 → OTP 발송(콘솔 로그 목업 가능, rate limit 적용) → 확인(5분 만료·1회용) → PIN 재설정
- [ ] 대시보드용 "진행 중인 채팅방" 조회 API (아이별 활성 `ChatRoom` 존재 시 노출)
- [ ] 112 신고 대체 경로 서버 처리 (finder가 위치 거부 시 시스템 메시지로 보호자에게 통지)
- [ ] 배포 호스트 선정 및 환경 세팅(HTTPS 포함), `BASE_URL` 배포 도메인으로 교체
- [ ] 도메인 확정 직후 `QrToken` 배치 생성 스크립트 실행(N개) + QR PNG·시리얼 목록을 디자인 팀원에게 전달
- [ ] 프론트와 1차 통합 — 실제 API로 회원가입/로그인/대시보드 화면 연결 확인
- 🔗 **넘겨줄 것**: 확정된 배포 도메인으로 생성한 QrToken 배치(QR 이미지 + 시리얼 목록) → 디자인 팀원이 QR 스티커 인쇄 진행할 수 있게 (늦어도 주 중반)

**Week 3**
- [ ] 배포 최종 점검(WebSocket 프록시, DB 영속성)
- [ ] 보안 점검: PIN 해싱/rate limit/채팅방 소유권 검증 정상 동작 확인, 개인정보 저장, `/admin/*` 접근 제한, 남용 방지
- [ ] 실기기 통합 테스트, 발견된 버그 수정
- [ ] 데모/백업 영상 촬영 및 편집
- [ ] 최종 리허설 기술 지원

---

### 프론트 · 컴공

**Week 1**
- [x] 보호자 등록/대시보드 UI 초안 (`/register`, 아이 카드/토글/삭제/QR 다운로드) — 완료 (develop 브랜치, 재사용)
- [ ] `register.html`에 **PIN 입력란** 추가
- [ ] **로그인 폼(`login.html`) 신규 마크업** (전화번호 + PIN)
- [ ] 대시보드를 세션 기반 라우팅(`/guardian/dashboard`)에 맞게 조정 + "진행 중인 채팅방" 표시 영역 추가 (마크업만 먼저, 임시 데이터로)
- [ ] 백엔드 팀원과 API 계약 정렬(엔드포인트/응답 스키마, 시리얼·로그인·PIN 재설정 실패 케이스 포함)
- [ ] `chat.html` 위치-잠금 UI 및 "위치 공유 / 112 신고" 두 버튼 화면 작업
- 🔗 **받을 것**: 백엔드의 API 스펙 (Week 1 후반)

**Week 2**
- [ ] 목업(인메모리 dict) 라우트를 실제 백엔드 API로 교체(회원가입, 로그인, 대시보드, 상태 토글, 아이 추가/삭제 연동)
- [ ] 시리얼/로그인/PIN 재설정 오류 처리 UI ("유효하지 않은 시리얼", "이미 등록된 QR입니다", "전화번호 또는 PIN이 올바르지 않습니다", "PIN을 잊으셨나요?" 흐름)
- [ ] QR 코드 다운로드/표시를 대시보드에 통합 (다운로드 링크 자체는 이미 있음, 실제 `qr_token` 연결만 확인)
- [ ] 기획·운영 팀원과 페어링 — 화면 카피/112 안내 문구를 템플릿에 함께 삽입 (코딩 실습)
- [ ] 디자인 팀원과 페어링 — 확정된 로고/컬러를 `style.css`에 함께 반영 (코딩 실습)
- [ ] 반응형 1차 점검
- 🔗 **넘겨줄 것**: 동작하는 회원가입/로그인/대시보드 화면 → 기획팀 리허설에서 실제로 써볼 수 있게

**Week 3**
- [ ] 여러 실기기(안드로이드/아이폰, 카메라 앱 다른 버전)로 QR 스캔 → 전체 흐름 테스트
- [ ] 통합 테스트에서 나온 버그 수정
- [ ] 최종 배포본 UI 점검, 리허설 기술 지원

---

### 기획·운영 · 비전공

**Week 1**
- [ ] 발표 시나리오 초안 (문제 정의 → 데모 흐름 → 마무리)
- [ ] 실종아동법·개인정보보호법 관련 이슈 한 페이지로 정리 (발표 Q&A 대비, "왜 비밀번호가 아니라 PIN이냐" 답변 포함)
- [ ] 핵심 화면 카피 초안 작성: 랜딩 안내문구, 위치 요청 문구, 112 안내 문구, SMS(가입/OTP) 문자 내용 (문서로 먼저 정리)
- 🔗 **넘겨줄 것**: 카피 초안 → 프론트 팀원과의 Week 2 페어링에 사용

**Week 2**
- [ ] 프론트 팀원과 페어링 — 정리한 카피를 템플릿(`.html`)에 직접 삽입 (코딩 실습)
- [ ] 법적/개인정보 요약을 Q&A 예상질문-답변 형태로 다듬기
- [ ] 1차 통합 리허설 진행 — 일정 조율, 시나리오대로 실제 흐름 실행, 어색한 지점 기록
- [ ] 리허설 피드백을 팀에 공유하고 우선순위 조정 제안

**Week 3**
- [ ] 발표 스크립트 최종본 완성
- [ ] 리허설 반복 진행, 시간 재기, Q&A 답변 다듬기
- [ ] 백업 데모 영상 내레이션/자막 문구 지원 (편집은 본인/백엔드 담당)
- [ ] 발표 당일 역할(발표자/시연자/QR 부착 담당) 확정

---

### 디자인·브랜드 · 비전공

**Week 1**
- [ ] 로고 시안 2~3개 스케치 — **컬러는 이미 develop 브랜치에 파스텔 팔레트/오프화이트 배경으로 정해져 있으니 처음부터 새로 고르지 말고 그 톤에 맞춰서** 진행 (프론트 팀원에게 실제 화면 확인해달라고 요청)
- [ ] QR 스티커 컨셉 스케치 — QR과 **시리얼 번호(사람이 읽고 타이핑하기 쉬운 서체)** 를 같이 배치하는 레이아웃 고려, 실물 소재(방수 스티커 vs 열전사 프린팅) 후보 조사, 이미 정해진 파스텔 톤 반영
- [ ] 발표자료(PPT) 기본 골격/템플릿 잡기
- ⚠️ QR 스티커 최종 인쇄는 **배포 도메인이 확정되고 백엔드가 QrToken 배치를 생성한 후**에만 가능 — Week 1에는 컨셉까지만

**Week 2**
- [ ] 로고/컬러 확정 (프론트 팀원에게 전달)
- [ ] 🔗 **받는 즉시**: 백엔드가 만든 QrToken 배치(QR PNG + 시리얼 목록)로 QR 스티커 최종 디자인 완성 → **인쇄 발주** (등록 여부와 무관하게 배치 전체를 한 번에 진행, 인쇄 리드타임 고려해 주 중반까지는 발주)
- [ ] 프론트 팀원과 페어링 — `style.css`에서 색상/버튼/간격 값 직접 조정 (코딩 실습)
- [ ] 대시보드/채팅 화면 실제 스크린샷으로 발표자료 슬라이드 채우기 시작

**Week 3**
- [ ] 인쇄된 스티커 실물 수령 → 옷/천에 실제 부착 테스트
- [ ] 발표자료 최종 스크린샷/플로우로 완성
- [ ] 시연 소품(스티커 부착된 옷) 준비
- [ ] 리허설 참여 — 소품 담당 및 발표자료 큐 지원

---

## 발표 당일 "실사용 가능"의 최소 조건

- [ ] 고정 도메인 확정 (Render/Railway/Fly.io 무료 티어 또는 구매 도메인) — **QR 인쇄 전에 반드시 먼저 확정**
- [ ] HTTPS 적용 — Geolocation API는 HTTPS 또는 localhost가 아니면 브라우저가 막음
- [ ] `main.py`의 `BASE_URL`을 실제 배포 도메인으로 교체
- [ ] QrToken 배치를 **최종 배포 도메인 기준으로** 생성했는지 확인 (테스트용 로컬 도메인으로 만든 배치를 그대로 인쇄하지 않도록 주의)
- [ ] WebSocket이 배포 환경에서도 열리는지 확인 (일부 무료 호스팅은 WS 프록시 설정 별도 필요)
- [ ] DB 파일이 재배포/재시작 시 날아가지 않는지 확인 (영구 볼륨 또는 배포 직전 최종본 고정)
- [ ] 실물 QR 스티커로 실제 스마트폰 카메라 스캔 테스트 (에뮬레이터 아닌 실기기)
- [ ] PIN 해싱·로그인 rate limit·채팅방 소유권 검증이 실제로 동작하는지 배포 환경에서 최종 확인

---

## 남은 열린 질문

- **실제 SMS API 도입 여부** — PIN 재설정 OTP는 최소한 콘솔 로그 목업으로 데모 가능. 실사용을 강조하려면 비용을 감수하고 실제 연동을 붙일지 팀 논의 필요
