# 3주차 백엔드 신규 기능 — 프론트 추가구현 요청

**작성 배경**: 3주차에 "채팅방에 보호자를 추가로 초대할 수 있는 기능"(가족 초대)을 백엔드에 구현했습니다. 백엔드 라우트는 모두 완성·검증됐고, `guardian_dashboard.html`에 라우트 연결 확인용 최소 마크업(디자인 없음)을 임시로 넣어뒀습니다. 이 문서는 그 최소 마크업을 실제 디자인 시스템에 맞게 다시 만들어달라는 요청입니다.

---

## 기능 개요

아이 한 명에 보호자를 여러 명 연결할 수 있게 됐습니다(예: 부모 두 명이 같은 아이를 함께 관리).

- 아이를 처음 등록한 사람("최초 등록자")이 **초대 코드**(8자리, 예: `AB3D9KMP`)를 발급합니다.
- 그 코드를 문자/카톡 등으로 다른 보호자에게 직접 전달합니다(서버가 대신 보내지 않습니다).
- 초대받은 사람은 로그인 후 대시보드에서 코드를 입력하면 그 아이가 자기 목록에도 나타납니다.
- 코드는 24시간 후 만료되고, 1회 사용하면 즉시 소멸합니다.
- 두 사람 모두 채팅 열람·실종 신고 토글·QR 다운로드는 동일하게 할 수 있습니다. **아이 삭제와 초대 코드 발급만 최초 등록자에게 제한**됩니다(초대받은 사람의 실수나 다툼으로 아이 데이터가 통째로 사라지는 사고를 막기 위함).

---

## 지금 상태 (백엔드 완료, 프론트 디자인 미완성)

`templates/guardian_dashboard.html`에 아래 마크업이 이미 들어가 있습니다. 기존 `alert`/`destructive`/`button-outline` 등 클래스를 재사용했을 뿐, 새 디자인은 적용하지 않았습니다.

1. 아이 카드마다 `child.is_primary`가 true일 때만 보이는 "가족 초대하기" 버튼(`POST /guardian/dashboard/children/{child_id}/invite`)
2. 초대 코드 발급 성공 시 코드 값을 보여주는 배너
3. 초대 코드 입력 성공/실패 안내
4. "초대 코드로 합류하기" 폼(`POST /guardian/dashboard/join`, `code` 필드 하나)

---

## 요청 사항

### 1. 가족 초대하기 버튼 & 코드 발급 결과 표시

- 지금은 그냥 `button-outline` 텍스트 버튼입니다. 디자인 시스템에 맞는 아이콘(예: 사람 추가 아이콘)과 함께 배치해 주세요.
- 코드가 발급되면 `guardian_dashboard.html`이 받는 `invite_code` 값을 화면에 보여주는데, 지금은 그냥 `<strong>` 텍스트입니다. **복사 버튼**을 붙여서 문자/카톡으로 바로 붙여넣기 좋게 해주시면 좋겠습니다. 코드는 24시간 후 만료되고 1회용이라는 안내 문구도 눈에 잘 띄게 배치해 주세요.

### 2. 초대받지 않은(비-primary) 보호자에 대한 안내

- 지금은 `is_primary`가 아니면 초대 버튼을 그냥 숨기기만 합니다. 대신 "이 아이는 다른 가족이 등록했습니다" 같은 안내를 카드 안에 넣어주면 사용자가 왜 버튼이 없는지 헷갈리지 않을 것 같습니다. (필수는 아니고 제안입니다.)

### 3. 초대 코드로 합류하기 폼 & 에러 메시지

- `guardian_dashboard.html`에 전역 섹션으로 "초대 코드로 합류하기" 폼이 있습니다(코드 입력창 + 버튼 하나). 디자인 시스템에 맞게 다시 배치해 주세요.
- 서버가 돌려주는 에러 코드는 `join_error` 쿼리 파라미터로 4가지입니다. 각각 사용자 친화적인 문구로 디자인해 주세요.

| `join_error` 값 | 의미 | 제안 문구 |
|---|---|---|
| `missing_code` | 코드를 입력 안 하고 제출 | "초대 코드를 입력해 주세요." |
| `invalid_code` | 존재하지 않는 코드 | "존재하지 않는 코드입니다. 다시 확인해 주세요." |
| `expired_code` | 만료됐거나 이미 사용된 코드 | "만료되었거나 이미 사용된 코드입니다. 가족에게 새 코드를 요청해 주세요." |
| `already_linked` | 이미 연결된 아이(자기 자신의 코드를 입력한 경우 등) | "이미 함께 관리하고 있는 대상자입니다." |

- 성공 시(`joined=success`)에는 "가족 구성원으로 합류했습니다" 같은 확인 메시지와 함께, 새로 추가된 아이가 목록 어디에 있는지 시각적으로 강조(예: 잠깐 하이라이트 애니메이션)해주면 좋을 것 같습니다.

### 4. (제안) 채팅방 내 다중 보호자 인지 UI

- 백엔드는 이제 같은 아이에 보호자가 여러 명 동시에 채팅방에 접속하는 걸 허용합니다. 하지만 지금 채팅 화면(`chat.html`/`chat.js`)에는 "다른 보호자도 지금 보고 있다"는 표시가 전혀 없습니다.
- 발견자 입장에서는 자신이 보낸 메시지를 여러 보호자가 동시에 볼 수 있다는 걸 알 필요는 없지만, **보호자 입장에서는** "이미 다른 가족이 대응 중이다"를 아는 게 중복 대응(예: 둘 다 112 신고)을 막는 데 도움이 될 수 있습니다.
- 지금 당장 필수는 아니지만, 여유가 되면 "OO님도 이 채팅방에 있습니다" 같은 간단한 시스템 메시지나 상태 표시를 고려해 주시면 좋겠습니다. (참고: 서버는 이미 발견자/보호자 입장·퇴장 시 시스템 메시지를 브로드캐스트하고 있어서, `chat.js`가 이 메시지를 받아 표시만 하면 별도 백엔드 작업 없이도 가능합니다.)

---

## 회원가입 화면 변경 (QR 등록 분리 + 개인정보 동의)

**작성 배경**: 회원가입과 QR(태그) 등록을 한 번에 처리하던 걸 분리했습니다. `register.html`에도 최소 마크업만 임시로 반영해뒀습니다.

### 지금 상태 (백엔드 완료, 프론트 디자인 미완성)

- `/register`는 이제 **전화번호 + PIN만** 받아 계정을 만듭니다. 기존에 있던 "첫 등록 대상자"(대상자 이름 + 태그 일련번호) 입력란은 **완전히 제거**했습니다 — QR 등록은 가입 후 대시보드의 기존 "태그 추가" 폼에서 진행합니다.
- 계정 필드 아래에 **개인정보 수집·이용 동의** 섹션을 추가했습니다. 수집 항목(전화번호), 수집 목적, **"AI가 채팅 메시지 내용을 자동으로 읽고 분석할 수 있다"는 고지**, 보유 기간을 나열한 목록 + 체크박스 하나(`privacy_consent`)입니다. 지금은 순수 텍스트 목록 + 기본 HTML 체크박스라 디자인이 전혀 없습니다.
- 체크하지 않으면 서버가 `consent_required` 오류로 가입 자체를 막습니다(프론트뿐 아니라 서버에서도 재검증하므로, 이 체크박스의 `name="privacy_consent"`/`value="true"`는 그대로 유지해 주세요).

### 요청 사항

1. **개인정보 동의 섹션 디자인** — 지금은 `<ul>` 텍스트 목록 + 기본 체크박스입니다. 다른 폼 필드들과 톤이 맞게 카드/구분선 등으로 정리해 주세요. 특히 "AI가 채팅 내용을 읽는다"는 고지는 사용자가 놓치지 않도록 눈에 띄게 배치해 주시면 좋겠습니다(법적으로 중요한 고지라 작은 글씨로 숨기듯 넣으면 안 됩니다).
2. **가입 완료 후 안내 문구 점검** — intro 카피를 "태그(QR) 등록은 로그인 후 대시보드에서 진행합니다"로 바꿔뒀는데, 실제 화면 흐름(가입 → 대시보드 → 태그 추가)에 맞게 문구를 다듬어 주세요.
3. **오류 메시지**: `error=consent_required`일 때 "개인정보 수집·이용에 동의해야 가입할 수 있습니다." 문구가 나갑니다. 기존 `alert destructive` 컴포넌트 그대로 써도 되고, 필요하면 문구만 다듬어 주세요.

기존 `error=invalid_serial`/`serial_used`는 회원가입 화면에서는 더 이상 안 나옵니다(그 두 오류는 이제 대시보드의 "태그 추가" 폼에서만 발생합니다 — 1번 문서 참고 없이도 기존 대시보드 오류 처리 그대로 유지하시면 됩니다).

---

## 채팅 화면: 장소 검색으로 위치 직접 지정

**작성 배경**: 실내처럼 GPS/WiFi 위치 오차가 아주 큰 상황(테스트에서 2000m까지 나옴)에서, 발견자가 브라우저 GPS만 믿지 않고 장소 이름을 검색해서 좌표를 직접 지정할 수 있는 대안을 추가했습니다. 카카오 로컬 API(장소 검색) 호출은 서버가 대신 하고, 프론트는 결과 목록만 받아 보여주면 됩니다. `chat.html`/`chat.js`/`style.css`에 최소 기능 마크업만 반영해뒀습니다(디자인 없음).

### 지금 상태 (백엔드 완료, 프론트 디자인 미완성)

- 위치 공유 게이트 화면(`locationGate`)에 "GPS가 부정확하면 장소 직접 검색하기"라는 텍스트 링크 버튼이 추가돼 있습니다. 누르면 검색창(`#locationSearchBox`)이 펼쳐집니다.
- 검색어를 입력하고 검색하면 `GET /api/place-search?query=...`를 호출해 결과를 받아 `<ul>` 목록으로 보여줍니다(지금은 기본 리스트 스타일).
- 결과 중 하나를 클릭하면 그 장소의 좌표로 `location` 메시지를 전송하고, 위치 공유 게이트가 해제되어 채팅이 열립니다(GPS로 공유한 것과 동일하게 동작).
- 검색으로 지정한 위치는 채팅창에서 오차 반경(`오차 반경 약 XXm`) 대신 **장소 이름 + "검색으로 지정한 위치"** 문구로 표시됩니다(`renderLocation`이 이미 처리해둠).

### 요청 사항

1. **"장소 직접 검색하기" 진입 버튼** — 지금은 `btn-text-link` 텍스트 버튼입니다. GPS 공유 버튼(`shareLocationBtn`)보다는 눈에 덜 띄되, "위치가 이상하면 여기를 눌러보세요" 정도의 느낌으로 배치·문구를 다듬어 주세요.
2. **검색창 + 결과 목록 디자인** — 지금은 기본 `<input>`/`<button>`/`<ul><li>`입니다. 검색 결과가 여러 개 나올 수 있어서(카카오 API가 최대 10개 반환), 스크롤 가능한 리스트나 카드 형태로 정리해 주시면 좋겠습니다. 각 결과는 장소명 + 주소가 함께 보여야 합니다.
3. **검색 중/결과 없음 상태 표시** — 지금은 검색 버튼이 잠깐 비활성화되는 것 외에 로딩 표시가 없고, 결과가 없으면 "검색 결과가 없습니다" 텍스트만 나옵니다. 로딩 스피너나 빈 상태 일러스트 등을 붙여주시면 좋겠습니다.
4. **채팅창에 표시되는 "검색으로 지정한 위치" 라벨** — `renderLocation`이 이미 GPS 위치와 구분해서 보여주도록 짜여 있지만(장소명을 굵게, 아래에 "검색으로 지정한 위치 · 지도에서 보기"), 이 부분도 기존 `location-bubble` 디자인 톤에 맞게 다듬어 주세요.

### 참고: 카카오 REST API 키 관련

- 서버 환경변수 `KAKAO_REST_API_KEY`가 없으면 검색 자체가 503으로 응답합니다(프론트는 이 경우 "장소 검색 기능을 사용할 수 없습니다" 정도로만 안내하면 됩니다 — 이 키는 배포 환경에도 설정돼 있어야 합니다).

---

## 백엔드 API 참고

프론트 작업 시 참고할 라우트 요약입니다(전체 계약은 `API_CONTRACT.md` 참고).

| 메서드/경로 | 설명 | 성공 시 리다이렉트 |
|---|---|---|
| `POST /guardian/dashboard/children/{child_id}/invite` | 초대 코드 발급(최초 등록자만) | `/guardian/dashboard?invite_code=XXXXXXXX` |
| `POST /guardian/dashboard/join` (form: `code`) | 초대 코드로 아이에 합류 | `/guardian/dashboard?joined=success` |
| `GET /api/place-search?query=...` | 카카오 로컬 API 장소검색 프록시. 응답: `{"results": [{"name", "address", "latitude", "longitude"}, ...]}` | 리다이렉트 없음(WebSocket과 별개인 일반 fetch용 JSON API) |

`guardian_dashboard.html`이 받는 템플릿 변수:

- `children[].is_primary` (bool) — 이 보호자가 최초 등록자인지
- `invite_code` (str | None) — 방금 발급된 코드(1회만 표시, 새로고침하면 사라짐)
- `join_error` (str | None) — 위 표의 4가지 값 중 하나
- `joined` (str | None) — `"success"`면 합류 성공

---

## 문의

라우트 동작이나 응답 형식에 대해 궁금한 점이 있으면 백엔드 담당자에게 문의해 주세요. 위 4가지 라우트는 모두 실제 서버로 e2e 테스트를 완료한 상태입니다(발급 → 다른 계정으로 합류 → 채팅 접근 → 코드 재사용 차단 → 권한 구분까지 확인됨).
